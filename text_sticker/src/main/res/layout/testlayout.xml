<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ImageView
        android:id="@+id/mticking"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_textcolor"
        app:layout_constraintRight_toLeftOf="@+id/add_text_edit_text"
        app:layout_constraintEnd_toStartOf="@+id/add_text_edit_text"
        app:tint="@color/black" />

    <EditText
        android:id="@+id/add_text_edit_text"
        style="@style/EditText"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/add_text_color_picker_relative_layout"
        android:layout_centerInParent="true"
        android:background="@drawable/border_shape"
        android:focusable="true"
        android:gravity="center"
        android:hint="write text"
        android:imeOptions="actionDone"
        android:inputType="textNoSuggestions"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/mticking"
        app:layout_constraintHeight_percent="0.06"
        app:layout_constraintWidth_percent="0.06"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/ic_done"
        app:layout_constraintLeft_toRightOf="@+id/add_text_edit_text"
        app:layout_constraintStart_toEndOf="@+id/add_text_edit_text"
        app:tint="@color/black" />


</androidx.constraintlayout.widget.ConstraintLayout>